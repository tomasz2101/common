all:
  children:
    nas:
      hosts:
        192.168.0.102:
  vars:
    ansible_ssh_user: tom
    
    _image:
      tag: 0.0.1
    _namespace: prod

    services:
      namespace: "{{ _namespace }}"

      postgres:
        image: "{{ _image }}"
      
      influxdb:
        image: "{{ _image }}"
